import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
    image: {
        flex: 1,
        width: '100%',
        position: 'absolute',
        zIndex: -1,
        resizeMode: 'cover'
    }
});


export default styles;